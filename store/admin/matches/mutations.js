const mutations = {
  setFootballMatches(state, matches) {
    state.matches = matches;
  },

  setFootballMatch(state, match) {
    state.match = match;
  },
};

export default mutations;
